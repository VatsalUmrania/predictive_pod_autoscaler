# NEXUS Self-Healing SDK
# Real-world install: pip install nexus-selfheal
# Demo: editable install from local repo so any source change is reflected immediately
-e ../../.[nexus]

# Demo backend
aiosqlite>=0.20.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.0.0
pyyaml>=6.0
nats-py>=2.6.0
