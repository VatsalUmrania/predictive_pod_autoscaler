# AWS AI DevOps Agent — Dependencies
# Install with: pip install -r requirements-aws-agent.txt

# AWS
# boto3>=1.34.0 (Included in Lambda runtime)
# botocore>=1.34.0 (Included in Lambda runtime)

# LangGraph + LangChain
langgraph>=0.2.0
langchain-core>=0.2.0

# Bedrock is accessed via boto3 (included in Lambda runtime — no extra package needed)

# Web framework (Lambda handler via Mangum adapter)
fastapi>=0.111.0
mangum>=0.17.0          # ASGI adapter for AWS Lambda

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# HTTP client (for Slack webhook)
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0    # local .env support
