apiVersion: batch/v1
kind: CronJob
metadata:
  name: ppa-data-collector
  namespace: default
spec:
  schedule: "0 * * * *"    # Runs every hour
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
          - name: data-collector
            image: ppa-data-collector:latest
            imagePullPolicy: Never
            env:
            - name: TARGET_APP          # ← Change this to switch projects
              value: "test-app"
            - name: PROMETHEUS_URL      # ← Internal cluster DNS, no port-forward
              value: "http://prometheus-kube-prometheus-prometheus.monitoring.svc.cluster.local:9090"
            - name: NAMESPACE
              value: "default"
            - name: CONTAINER_NAME
              value: "nginx"
            - name: OUTPUT_PATH
              value: "/data/training_data.csv"
            volumeMounts:
            - name: training-data
              mountPath: /data
          volumes:
          - name: training-data
            persistentVolumeClaim:
              claimName: ppa-training-data-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ppa-training-data-pvc
  namespace: default
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
